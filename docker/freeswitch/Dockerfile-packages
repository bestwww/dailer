# 🐳 FreeSWITCH Docker - ГОТОВЫЕ ПАКЕТЫ (БЫСТРАЯ УСТАНОВКА!)
# Используем официальные пакеты SignalWire вместо сборки из исходников
FROM ubuntu:22.04

# 📋 Метаданные
LABEL maintainer="Dailer Team"
LABEL description="FreeSWITCH for Dailer - using official packages"
LABEL version="1.10.12-packages"

# 🌍 Переменные окружения
ENV DEBIAN_FRONTEND=noninteractive
ENV TZ=UTC
ENV FREESWITCH_VERSION=1.10

# 📦 Установка базовых зависимостей
RUN apt-get update && \
    apt-get install -y \
        # Базовые утилиты
        wget \
        gnupg2 \
        gpg \
        lsb-release \
        ca-certificates \
        # Сетевые утилиты для диагностики
        netcat-openbsd \
        telnet \
        net-tools \
        iputils-ping \
        # Утилиты для работы
        curl \
        vim \
        htop && \
    echo "✅ Базовые зависимости установлены"

# 🔑 Добавляем официальный ключ и репозиторий FreeSWITCH (современный способ)
RUN echo "🔑 Добавляем официальный репозиторий FreeSWITCH..." && \
    mkdir -p /etc/apt/keyrings && \
    wget -qO- https://files.freeswitch.org/repo/deb/debian-release/pubkey.gpg | gpg --dearmor -o /etc/apt/keyrings/freeswitch.gpg && \
    echo "deb [signed-by=/etc/apt/keyrings/freeswitch.gpg] http://files.freeswitch.org/repo/deb/debian-release/ $(lsb_release -sc) main" > /etc/apt/sources.list.d/freeswitch.list && \
    echo "✅ Репозиторий FreeSWITCH добавлен (современный метод)"

# 📦 Обновляем список пакетов и устанавливаем FreeSWITCH
RUN echo "📦 Устанавливаем FreeSWITCH из готовых пакетов..." && \
    apt-get update && \
    apt-get install -y \
        # Основные пакеты FreeSWITCH
        freeswitch \
        freeswitch-meta-bare \
        freeswitch-conf-vanilla \
        # Необходимые модули для дайлера
        freeswitch-mod-commands \
        freeswitch-mod-console \
        freeswitch-mod-logfile \
        freeswitch-mod-dptools \
        freeswitch-mod-expr \
        freeswitch-mod-fifo \
        freeswitch-mod-hash \
        freeswitch-mod-db \
        freeswitch-mod-esf \
        freeswitch-mod-fsv \
        # SIP и медиа модули
        freeswitch-mod-sofia \
        freeswitch-mod-loopback \
        freeswitch-mod-conference \
        freeswitch-mod-dialplan-xml \
        # Event Socket (критично для интеграции!)
        freeswitch-mod-event-socket \
        # Аудио кодеки
        freeswitch-mod-sndfile \
        freeswitch-mod-native-file \
        freeswitch-mod-tone-stream \
        freeswitch-mod-lua \
        # Дополнительные полезные модули
        freeswitch-mod-say-en && \
    echo "✅ FreeSWITCH установлен из готовых пакетов!"

# 🗂️ Создаем необходимые директории
RUN mkdir -p \
        /var/lib/freeswitch/storage \
        /var/lib/freeswitch/recordings \
        /var/log/freeswitch \
        /etc/freeswitch && \
    echo "✅ Директории созданы"

# 👤 Создаем пользователя freeswitch если его нет
RUN if ! id "freeswitch" &>/dev/null; then \
        useradd --system --home-dir /var/lib/freeswitch --shell /bin/false freeswitch; \
    fi && \
    echo "✅ Пользователь freeswitch настроен"

# 🔧 Устанавливаем правильные права доступа
RUN chown -R freeswitch:freeswitch \
        /var/lib/freeswitch \
        /var/log/freeswitch \
        /etc/freeswitch \
        /usr/share/freeswitch && \
    echo "✅ Права доступа настроены"

# 🧹 Очистка для уменьшения размера образа
RUN apt-get clean && \
    rm -rf /var/lib/apt/lists/* && \
    rm -rf /tmp/* && \
    rm -rf /var/tmp/* && \
    echo "🧹 Временные файлы очищены"

# 📂 Рабочая директория
WORKDIR /etc/freeswitch

# 🔌 Открываем необходимые порты
EXPOSE 5060/udp 5060/tcp 5080/udp 5080/tcp
EXPOSE 16384-32768/udp
EXPOSE 8021/tcp

# 🏥 Добавляем healthcheck
HEALTHCHECK --interval=30s --timeout=10s --start-period=60s --retries=3 \
    CMD fs_cli -x "status" | grep -q "UP" || exit 1

# 📝 Копируем entrypoint скрипт
COPY docker-entrypoint.sh /docker-entrypoint.sh
RUN chmod +x /docker-entrypoint.sh

# 🚀 Точка входа
ENTRYPOINT ["/docker-entrypoint.sh"]
CMD ["freeswitch", "-nonat", "-c"] 